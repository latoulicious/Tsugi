---
time: 11:02 AM
type: docs
breaking_change: false
modules:
  - docs/wiki
---

# Summary

Added a **gRPC learning-plan** doc — a deliberate learning fork
(`feat/grpc-agent`), explicitly *not* roadmap. Captures how a control-plane /
per-host-agent split over gRPC maps onto Tsugi's existing deploy seam
(`cli.Deployer` + `deploy.Script` shelling `deploy.sh`), so the gRPC concepts
(unary → server-stream → bidi → mTLS) get learned against real code instead of a
toy. Overkill for one VPS on purpose; the single-binary deployer stays the
shipping path.

# Files Touched

- `docs/wiki/grpc-learning-plan.md` — new. Topology diagram (1 agent per host,
  not per project; fan-out = loop over the single-target path), the
  `cli.Deployer` seam, verb→RPC mapping, P0–P5 phases (start at P1 `GetVersion`
  unary reusing `version.Get()`), lazy guardrails.
- `docs/wiki/README.md` — index row for the new doc.

# Notes

- Flagged "learning fork, not roadmap" in title + index so it isn't mistaken for
  approved work. No code written; `feat/grpc-agent` not yet branched.
