# Tsugi staging interpolation vars for the lazyscan stack (placeholders).
# The api's own secrets live in LazyScan/api/.env — do not duplicate them here.
PUBLIC_APP_URL=https://staging-api.example.com
PUBLIC_S3_URL=https://staging-s3.example.com
POSTGRES_PASSWORD=change-me
MINIO_ROOT_USER=change-me
MINIO_ROOT_PASSWORD=change-me
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM=noreply@staging.example.com
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_STARTTLS=true
